public class PersonTest {
    public static boolean isTeenager(int age)
    {
        boolean result;
        if (age < 19) {
            result = true;
        }
        else {
            result = false;
        }
        return result;
    }
    System.out.println(result);
}
@test1
 void testAge(){
    int age = 20;
    assertFalse(result);
}
